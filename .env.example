# Aviation Matrix 360 — environment variable reference
# Copy this file to .env and fill in real values (never commit .env)

# Required by Replit workflows — set automatically in the Replit environment
PORT=3000

# Optional: set BASE_PATH if serving from a subpath (default: /)
BASE_PATH=/
